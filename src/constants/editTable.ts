export enum EditColumnOperationType { 
  // 新增
  Add = 'ADD',
  // 修改
  Modify = 'MODIFY',
  // 删除
  Delete = 'DELETE',
}

// nullable
export enum NullableType {
  // 不可为空
  NotNull = 'NO',
  // 可为空
  Null = 'YES',
}
